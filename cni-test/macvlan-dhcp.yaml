apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: macvlan-dhcp
spec:
  config: '{
      "cniVersion": "0.3.0",
      "type": "macvlan",
      "master": "env32",
      "mode": "bridge",
      "ipam": {
        "type": "dhcp"
      }
    }'
